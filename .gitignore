# local save backups (personal run data - never commit)
backups/

# scratch / temp files
_*.save
_loc.txt
*.tmp
*.bak

# crash log written next to the editor at runtime
crash-log.txt

# per-save edit stamps for last-writer detection (local state)
.last-edits.json

# editor UI preferences (local state)
.editor-prefs.json
